import { PropsWithChildren } from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
export const colors = { ink: '#20312E', green: '#315F55', cream: '#F7F3EA', white: '#FFFFFF', muted: '#687773', gold: '#D89A4A', border: '#DDE3DD' };
export const Screen = ({ children }: PropsWithChildren) => <View style={styles.screen}>{children}</View>;
export function StepHeader({ step, title, onBack }: { step: string; title: string; onBack?: () => void }) { return <View style={styles.header}>{onBack ? <Pressable accessibilityRole="button" onPress={onBack} hitSlop={12}><Text style={styles.back}>‹ Atrás</Text></Pressable> : <View />}<Text style={styles.step}>{step}</Text><Text style={styles.title}>{title}</Text></View>; }
export function PrimaryButton({ label, onPress, disabled = false }: { label: string; onPress: () => void; disabled?: boolean }) { return <Pressable accessibilityRole="button" accessibilityState={{ disabled }} disabled={disabled} onPress={onPress} style={({ pressed }) => [styles.primary, disabled && styles.disabled, pressed && !disabled && styles.pressed]}><Text style={styles.primaryText}>{label}</Text></Pressable>; }
export function Choice({ selected, label, onPress }: { selected: boolean; label: string; onPress: () => void }) { return <Pressable accessibilityRole="radio" accessibilityState={{ selected }} onPress={onPress} style={[styles.choice, selected && styles.choiceSelected]}><Text style={[styles.choiceText, selected && styles.choiceTextSelected]}>{selected ? '✓ ' : ''}{label}</Text></Pressable>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.cream }, header: { paddingHorizontal: 24, paddingTop: 16, paddingBottom: 12 }, back: { color: colors.green, fontSize: 18, fontWeight: '700', marginBottom: 14 }, step: { color: colors.green, fontSize: 14, fontWeight: '800', letterSpacing: 1.2, textTransform: 'uppercase' }, title: { color: colors.ink, fontSize: 32, lineHeight: 38, fontWeight: '800', marginTop: 5 }, primary: { minHeight: 60, backgroundColor: colors.green, borderRadius: 18, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 22 }, primaryText: { color: colors.white, fontSize: 20, fontWeight: '800' }, disabled: { opacity: 0.45 }, pressed: { transform: [{ scale: 0.98 }] }, choice: { minHeight: 52, borderWidth: 2, borderColor: colors.border, backgroundColor: colors.white, borderRadius: 14, padding: 14, justifyContent: 'center' }, choiceSelected: { borderColor: colors.green, backgroundColor: '#E8F1EE' }, choiceText: { color: colors.ink, fontSize: 17, fontWeight: '700' }, choiceTextSelected: { color: colors.green } });
